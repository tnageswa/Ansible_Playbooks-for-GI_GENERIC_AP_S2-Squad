---
 - name: update SUSE patches
   hosts: all
   become: yes
   become_user: ansadmin
   any_errors_fatal: false
   tasks:
   - name: Create a file
     file:
      path: /etc/zypp/file1
      state: touch
      owner: ansadmin
      group: ansadmin
      mode: '0644'
      
      
      
      
      
    
