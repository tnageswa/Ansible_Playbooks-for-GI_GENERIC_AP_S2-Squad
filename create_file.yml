---
 - name: update SUSE patches
   hosts: all
   remote_user: ansadmin
   become: yes
   any_errors_fatal: false
   tasks:
   - name: Create a file
     file:
      path: /etc/zypp/file1
      state: touch
      mode: '0644'
      
      
      
      
      
    
